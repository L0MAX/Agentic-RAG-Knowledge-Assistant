# RAG pipeline

Not implemented yet. Phase 1 only reserves ingestion, retrieval, and embedding configuration.

Planned flow:

```text
Upload → Validate → Extract → Normalize → Chunk → Embed → Store (pgvector) → Retrieve → Grounded answer + citations
```

Details will be added in Phases 5–10 and 15.
