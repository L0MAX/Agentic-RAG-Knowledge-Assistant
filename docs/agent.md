# Agent

Not implemented yet. Phase 1 only reserves `backend/app/agents/` and LLM config keys.

The assistant will be a tool-calling agent, not a hard-coded retrieve-then-generate pipeline. Planned tools:

- `search_knowledge_base`
- `get_document`
- `get_document_section`
- `list_knowledge_sources`

See `TODO.md` phases 11–13.
